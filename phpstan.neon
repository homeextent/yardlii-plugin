includes:
    - phpstan-baseline.neon

parameters:
    level: 6
    paths:
        - includes
    bootstrapFiles:
        - phpstan-bootstrap.php
    scanFiles:
        - yardlii-core-functions.php
    # This is the new way to ignore the warning
    ignoreErrors:
        -
            identifier: missingType.iterableValue