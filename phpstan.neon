# phpstan.neon
includes:
    - phpstan-baseline.neon

parameters:
    level: 6
    paths:
        - includes
        - tests/unit

    excludePaths: # <-- FIX: Removed the incorrect 'analyse:' key
        - vendor/*
        - assets/*
        - tests/integration/*

    bootstrapFiles:
        - tests/phpstan-bootstrap.php

    reportUnmatchedIgnoredErrors: false